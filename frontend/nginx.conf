server {`n    listen 80;`n    server_name localhost;`n`n    location / {`n        root /usr/share/nginx/html;`n        index index.html index.htm;`n        try_files $uri $uri/ /index.html;`n    }`n}
